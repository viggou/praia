// Uses the built-in math namespace (no grain import needed)

func circleArea(r) {
    return math.PI * r * r
}

func distance(x1, y1, x2, y2) {
    let dx = x2 - x1
    let dy = y2 - y1
    return math.sqrt(dx * dx + dy * dy)
}

export { circleArea, distance }
